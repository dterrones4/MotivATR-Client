import React from 'react';
import {shallow} from 'enzyme';

import { SetMotivatr } from './set-motivatr';

describe('<SetMotivatr />', () => {
    it('Renders without crashing', () => {
        shallow(<SetMotivatr />)
    });
});
